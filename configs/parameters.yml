models: [
  null, 
  # 'random_forest', 
  # 'gradient_boosting',
  # 'neural_network'
]

train_window: [
  # 11520, # dos anios
  # 5760, # un anio
  # 1920, # cuatro meses
  # 1440, # tres meses
  # 1200, # dos meses y medio
  # 960, # dos meses
  # 720, # mes y medio
  # 480, # un mes
  # 360, # tres semanas 
  # 240, # dos semanas
  # 120, # una semana
  1,
] # tamanio de la ventana de entrenamiento

train_period: [
  24, 
  # 168, 
  # 480, 
] # frecuencia con la que se entrena

periods_forward_target: [
  # 4,
  # 8,
  # 12,
  # 16,
  24,
  # 48,
  # 10,
]

trading_strategy: [
  # 'backbone.strategies.random.Random',
  # 'backbone.strategies.consecutive_candles.ConsecutiveCandlesV1',
  # 'backbone.strategies.consecutive_candles.ConsecutiveCandlesV2',
  # 'backbone.strategies.consecutive_candles.ConsecutiveCandlesV23',
  'backbone.strategies.consecutive_candles.ConsecutiveCandlesV24',
  # 'backbone.strategies.largest_candle.LargestCandle',
  # 'backbone.strategies.macd.MACD',
  # 'backbone.strategies.bbands_filter.BBandsFilter',
  # 'backbone.strategies.rmi_sniper.RMIMACDSniperV2',

  
]

intervals: [
  2,
  # 4,
  # 6,
  # 8
]

stop_loss_in_pips: [
  # 3,
  # 5,
  # 8,
  # 10,
  # 15,
  # 20,
  50,
  # 60,
  # 100
]

risk_reward_ratio: [
  0.001,
  # 0.0005,
  # 0.05,
  # 0.01,
  # 0.015,
  # 0.008,
  # 0.03,
  # 0.1,
  # 0.12,
]

leverages: [
  # 1,
  100,
  # 200,
  # 500,
]

risk_percentages: [
  # 1, 
  # 2, 
  3,
  # 4, 
  # 6, 
  # 10, 
  # 15, 
  # 20, 
  # 25, 
  # 30, 
  # 40, 
  # 50, 
  # 60
]

grid_sizes: [
  # 10, 
  # 15, 
  # 20, 
  30, 
  # 40, 
  # 50

]

trades_to_increment_risks: [
  0,
  # 1,
  # 2,
  # 3,
  # 4,
  # 5
]

multipliers: [
  0,
  # 1,
  # 1.2,
  # 1.5,
  # 1.8
]

start_lots: [
  1000,
  # 2000,
  # 3000, 
  # 4000, 
  # 5000
]
